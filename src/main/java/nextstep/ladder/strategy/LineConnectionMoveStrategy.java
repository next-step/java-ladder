package nextstep.ladder.strategy;

public class LineConnectionMoveStrategy implements LineConnectionStrategy{

    @Override
    public boolean createLine(boolean previousLine) {
        return true;
    }
}
