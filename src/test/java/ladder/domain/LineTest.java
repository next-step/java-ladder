package ladder.domain;

import org.assertj.core.api.Assertions;

import java.util.Arrays;

public class LineTest {
    /*
    Point point1 = Point.first(true).last();
    Point point2 = Point.first(false).last();
    Moving moving1 = new Moving(0, point1);
    Moving moving2 = new Moving(1, point1.next(false));
    Moving moving3 = new Moving(2, point1.next(true));
    Moving moving4 = new Moving(3, point1.last());

    Moving movingA = new Moving(0, point2);
    Moving movingB = new Moving(1, point2.next(true));
    Moving movingC = new Moving(2, point2.next(false));
    Moving movingD = new Moving(3, point2.last());

    Line line1 = Line.of(Arrays.asList(moving1, moving2, moving3, moving4));
    //Line line2 = Line.of(Arrays.asList(movingA, movingB, movingC, movingD));
    Assertions.assertThat(line1.move(0)).
    */

}
